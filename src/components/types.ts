export type CardProps = {
  icon: string;
  amount: number;
  amountType: string;
};
export interface DataType {
  key: React.Key;
  description: string;
  amount: number;
  transactionType: string;
}
